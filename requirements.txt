# Core dependencies
numpy>=1.21.0
scipy>=1.7.0

# Medical imaging
SimpleITK>=2.2.0
nibabel>=3.2.0
pydicom>=2.3.0

# Deep learning (for TotalSegmentator) - OPCIONAL
# Instale apenas se tiver GPU NVIDIA ou quiser usar CPU (mais lento)
# torch>=2.0.0
# torchvision>=0.15.0
# TotalSegmentator>=1.5.0
# NOTA: TotalSegmentator requer scikit-image, que precisa de compilador C/C++
# Veja README.md para instruções de instalação do Visual Studio Build Tools

# 3D mesh processing
trimesh>=3.15.0
# scikit-image>=0.19.0  # Requer compilador C/C++ no Windows
# Usaremos scipy.ndimage e trimesh para marching cubes como alternativa

# Utilities
tqdm>=4.62.0